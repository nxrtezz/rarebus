version: "3.9"

services:
  rarebus:
    build: .
    container_name: rarebus
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ./db.sqlite3:/app/db.sqlite3
    env_file:
      - .env
    restart: unless-stopped